services:
  - type: web
    name: expense-ai
    env: python
    buildCommand: cd expense_ai && pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --chdir expense_ai api_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
