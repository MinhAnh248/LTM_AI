services:
  - type: web
    name: expense-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gevent --workers 1 --bind 0.0.0.0:5000 api_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
